<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@mipmap/background_drawer"
    android:layout_width="match_parent"
    android:layout_height="190dp"
    tools:context=".views.TimeLine">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile_user"
        app:border_color="@color/colorPrimaryLight"
        app:border_width="3dp"
        android:src="@drawable/com_facebook_profile_picture_blank_square"
        android:layout_marginLeft="24dp"
        android:layout_marginStart="24dp"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_width="90dp"
        android:layout_height="90dp" />
    <TextView
        android:id="@+id/lbl_fullname_navigation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/user_full_name"
        android:textSize="14sp"
        android:textColor="@color/icons"
        android:textStyle="bold"
        android:gravity="left"
        android:paddingBottom="4dp"
        android:layout_above="@+id/lbl_email_navigation"
        android:layout_alignLeft="@id/profile_user"
        android:layout_alignStart="@id/profile_user"/>
    <TextView
        android:id="@+id/lbl_email_navigation"
        android:text="@string/user_emai_nave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="left"
        android:layout_marginBottom="8dp"
        android:textSize="14sp"
        android:textColor="@color/icons"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@id/lbl_fullname_navigation"
        android:layout_alignStart="@id/lbl_fullname_navigation"/>

</RelativeLayout>